[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "tap-it-ai-tools"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "openai",
    "python-dotenv",
    "pydantic",
    "typer"
]

[project.scripts]
tapit-ai = "tapit_ai.cli:app"